#!/bin/sh

if [ -n "$(git status 2> /dev/null)" ]; then
  branch=$(git branch | grep -v '^\*' | fzf --height=70% --border=sharp | tr -d '[:space:]')
  if [ -n "$branch" ]; then
    git checkout $branch
  fi
else
  echo 'Current directory is not a git repository.'
fi
